/**
 * Styling suggestions — proposed className changes to help submitted copy
 * fit better at various screen sizes. Mike reviews and accepts/keeps each one
 * via the Review Suggestions wizard. Accepted choices are submitted as a
 * pending file for the developer to apply.
 *
 * Each entry:
 *   id               — matches the EditableField id on the page
 *   label            — human-readable location
 *   reason           — why this needs attention (shown to reviewer)
 *   currentClassName — the className currently on that EditableField
 *   suggestedClassName — the proposed replacement
 *   note             — one-line explanation of what the change does
 */
export const SUGGESTIONS = [
  // No active suggestions at this time.
  // Add entries here when specific fields need responsive / sizing review.
]
